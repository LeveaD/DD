{
  "name": "disputedefend-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm run build",
    "test": "cd backend && npm test && cd ../frontend && npm test",
    "typecheck": "cd backend && npm run typecheck && cd ../frontend && npm run typecheck"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "groq-sdk": "^1.6.0",
    "pdfkit": "^0.20.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2"
  }
}
